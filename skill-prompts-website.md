# 完整網站設計練習提示詞

> 按照順序練習，從規劃到實作完整走一遍。
> 複製提示詞貼到 Gemini Canvas 或 Google AI Studio 即可開始。

---

## 第一階段：規劃

### 1-1 網站地圖 Sitemap
```
我要建立一個公司官方網站，包含以下功能：
- 公司簡介、服務介紹、產品販售、活動報名、最新消息、聯絡我們
- 使用者可以註冊會員、登入、購買產品、報名活動
- 管理員可以在後台管理產品、訂單、會員、文章、活動

請幫我畫出完整的網站地圖（Sitemap），用文字樹狀結構表示所有頁面和它們之間的關係。
並標示哪些頁面需要登入才能進入。
```

---

### 1-2 功能需求清單
```
根據以下網站地圖，幫我整理成一份功能需求清單表格：
欄位包含：頁面名稱、功能名稱、功能說明、優先順序（高/中/低）、需要登入（是/否）

網站頁面：
- 首頁：Hero Banner、服務亮點、最新活動、最新消息、合作夥伴
- 產品頁：產品列表、篩選分類、產品詳細頁、加入購物車
- 購物車：商品清單、數量調整、小計、結帳
- 活動報名頁：活動資訊、議程表、報名表單
- 最新消息：文章列表、文章詳細頁
- 聯絡我們：聯絡表單、地圖
- 會員中心：個人資料、訂單紀錄、報名紀錄
- 管理後台：產品管理、訂單管理、會員管理、文章管理
```

---

### 1-3 線框稿 Wireframe
```
請幫我用 ASCII 文字畫出以下頁面的線框稿（Wireframe），
只需要用方框和文字標示各區塊的位置和名稱，不需要實際樣式。

頁面：電商產品列表頁
需要包含：
- 頂部導覽列（Logo、選單、購物車圖示、登入按鈕）
- 左側篩選欄（分類、價格範圍、標籤）
- 右側產品格狀列表（每格含圖片、名稱、價格、加入購物車按鈕）
- 分頁控制
- 頁尾

請同時說明每個區塊的用途。
```

---

## 第二階段：前端頁面實作

### 2-1 首頁 Hero Banner
```
請用 React 幫我製作一個公司網站的首頁 Hero Banner 區塊，包含：
- 全螢幕背景（可以是漸層色或圖片）
- 左側：大標題、副標題、兩個 CTA 按鈕（了解更多 / 立即報名）
- 右側：一張示意圖片（用色塊代替）
- 向下捲動箭頭動畫

樣式要求：現代感、留白充足、按鈕有 hover 效果。
請附上完整 JSX 和 CSS 程式碼，並說明每個部分的用途。
```

---

### 2-2 服務亮點區塊
```
請用 React 製作網站首頁的「服務亮點」區塊，包含：
- 區塊標題：「我們提供的服務」
- 4 個服務卡片，每張卡片包含：圖示（用 emoji）、服務名稱、簡短說明
- 服務內容：活動規劃、產品販售、會員經營、數位行銷
- 卡片 hover 時有上浮效果

請用 CSS Grid 排版，手機版改為單欄。
附上完整程式碼和 RWD 說明。
```

---

### 2-3 關於我們頁面
```
請用 React 製作「關於我們」頁面，包含以下區塊：
1. 頁面標題 Banner（背景色塊 + 標題）
2. 公司故事：左圖右文排版，說明創立背景和使命
3. 核心數字：3 個數字亮點（成立年數、服務人次、舉辦活動場次）
4. 發展歷程時間軸：垂直時間軸，列出 4 個重要里程碑
5. 團隊介紹：3 位成員的照片圓形框、姓名、職稱

請提供完整程式碼，並說明時間軸的 CSS 做法。
```

---

### 2-4 最新消息列表頁
```
請用 React 製作「最新消息」文章列表頁，包含：
- 頁面標題
- 分類標籤篩選（全部、公告、活動、媒體報導）
- 文章卡片列表：封面圖、分類標籤、標題、摘要前 50 字、發布日期、閱讀更多按鈕
- 點擊分類標籤後篩選對應文章
- 分頁功能：每頁 6 篇

使用假資料陣列模擬 8 篇文章。請說明 filter 篩選的邏輯。
```

---

### 2-5 聯絡我們頁面
```
請用 React 製作「聯絡我們」頁面，包含：
1. 聯絡表單：姓名、Email、主旨（下拉選擇）、訊息（多行輸入）、送出按鈕
2. 表單驗證：必填欄位未填時顯示紅色提示
3. 送出後顯示「感謝您的來信，我們將盡快回覆」
4. 右側聯絡資訊：地址、電話、Email、辦公時間
5. 嵌入 Google Maps（用 iframe 方式）

請說明如何從 Google Maps 取得嵌入用的 iframe 程式碼。
```

---

## 第三階段：產品頁與購物車

### 3-1 產品列表頁
```
請用 React 製作一個電商產品列表頁，包含：
- 頂部：頁面標題、目前顯示筆數（共 12 項產品）
- 左側篩選欄：
  - 分類（全部、服飾、配件、書籍）
  - 價格範圍滑桿（0 ~ 5000 元）
  - 排序方式（最新、價格低到高、價格高到低）
- 右側產品卡片（Grid 排版）：
  - 產品圖片（用色塊代替）
  - 產品名稱
  - 價格（NT$）
  - 庫存狀態（有貨 / 售完）
  - 加入購物車按鈕（售完時 disabled）

使用假資料 12 筆，說明 filter 和 sort 的實作方式。
```

---

### 3-2 產品詳細頁
```
請用 React 製作產品詳細頁，包含：
- 左側：產品主圖 + 下方縮圖列（點縮圖切換主圖）
- 右側：
  - 產品名稱（大字）
  - 價格（NT$ 紅色大字）
  - 評分（星星圖示 + 評論數）
  - 規格選擇（顏色：3 個色塊可點選；尺寸：S / M / L / XL 按鈕）
  - 數量選擇（減號 / 數字 / 加號，最小值 1）
  - 加入購物車按鈕（紫色大按鈕）
  - 立即購買按鈕（白色外框按鈕）
- 下方：產品描述文字區塊
- 再下方：相關產品推薦（4 張卡片）

說明數量加減的 useState 控制方式。
```

---

### 3-3 購物車頁面
```
請用 React 製作購物車頁面，包含：
左側購物車清單：
- 每筆商品：圖片、名稱、規格（顏色/尺寸）、單價、數量加減、小計、刪除按鈕
- 全選/取消全選 checkbox
- 刪除選取商品按鈕
- 繼續購物連結

右側結帳摘要：
- 商品小計
- 運費（滿 1000 免運，否則 60 元）
- 折扣碼輸入框
- 合計金額（大字）
- 前往結帳按鈕

購物車為空時顯示「購物車是空的」提示和回到產品頁按鈕。

請說明如何用 useContext 或 useState 管理購物車的全域狀態。
```

---

### 3-4 購物車全域狀態管理
```
請用 React Context API 幫我建立一個購物車的全域狀態管理系統，需要包含：
- CartContext：儲存購物車商品陣列
- 加入購物車：如果商品已存在則增加數量，否則新增
- 移除商品：從購物車刪除指定商品
- 修改數量：更新指定商品的數量
- 清空購物車
- 計算總數量（顯示在導覽列購物車圖示的 badge）
- 計算總金額

請提供完整的 CartContext.jsx 程式碼，並示範如何在產品頁和購物車頁使用它。
```

---

### 3-5 結帳流程
```
請用 React 製作結帳流程頁面，分成三個步驟：

步驟一：填寫收件資料
- 姓名、電話、Email
- 縣市下拉選單、鄉鎮市區下拉選單（依縣市連動）
- 詳細地址
- 備註

步驟二：選擇付款方式
- 信用卡（輸入卡號、有效期限、CVV）
- 超商取貨付款
- 銀行轉帳

步驟三：確認訂單
- 顯示商品清單、收件資料、付款方式
- 確認下單按鈕

頂部顯示步驟進度條（1 → 2 → 3）。
說明多步驟表單的 useState 控制方式。
```

---

## 第四階段：後台管理

### 4-1 產品管理後台
```
請用 React 製作管理員的產品管理頁面，包含：
- 頂部：搜尋框、新增產品按鈕
- 產品列表表格：勾選框、圖片、名稱、分類、價格、庫存、狀態、操作（編輯/刪除）
- 分頁（每頁 10 筆）
- 點擊新增/編輯時出現 Modal 表單：產品名稱、分類、價格、庫存數量、描述、上傳圖片、是否上架
- 刪除時出現確認對話框

說明 Modal 的開關控制和表單的 controlled component 寫法。
```

---

### 4-2 訂單管理後台
```
請用 React 製作管理員的訂單管理頁面，包含：
- 篩選列：訂單狀態（全部、待付款、處理中、已出貨、已完成、已取消）、日期範圍
- 訂單列表表格：訂單編號、客戶姓名、商品數量、總金額、付款方式、訂單狀態、下單時間、操作
- 點擊訂單可展開詳細內容：商品清單、收件資料
- 可更改訂單狀態（下拉選單）

使用假資料 15 筆訂單，說明狀態篩選和日期篩選的實作。
```

---

## 第五階段：RWD 手機版

### 5-1 導覽列手機版
```
請幫我將以下電腦版導覽列改成支援手機版的 RWD 設計：
電腦版：左側 Logo、右側選單連結（首頁、產品、活動、消息、聯絡）+ 登入按鈕 + 購物車圖示
手機版：左側 Logo、右側漢堡選單按鈕；點擊後從上方滑下全螢幕選單

請提供完整的 React + CSS 程式碼，說明 useMediaQuery 或 CSS media query 的使用方式。
```

---

### 5-2 全站 RWD 檢查清單提示
```
我已經完成一個包含首頁、產品頁、購物車、聯絡我們的網站。
請幫我列出一份完整的 RWD 手機版檢查清單，包含：
- 哪些 CSS 屬性最常需要在手機版調整
- 圖片在手機版的處理方式
- 表格在手機版的處理方式
- 表單在手機版的處理方式
- 常見的 RWD 錯誤和如何避免

並告訴我如何在 Chrome 開發者工具中模擬各種手機螢幕尺寸來測試。
```

---

## 第六階段：後端 API

### 6-1 產品 API
```
請用 Node.js + Express 幫我建立產品相關的 API，包含：
- GET /api/products：取得所有產品（支援 ?category=服飾&sort=price_asc&page=1&limit=12 篩選）
- GET /api/products/:id：取得單一產品詳細資料
- POST /api/products：新增產品（需管理員權限）
- PUT /api/products/:id：更新產品（需管理員權限）
- DELETE /api/products/:id：刪除產品（需管理員權限）

使用假資料陣列模擬資料庫，說明各 API 的用途和如何用 Postman 測試。
```

---

### 6-2 購物車與訂單 API
```
請用 Node.js + Express 幫我建立購物車和訂單 API，包含：
- POST /api/orders：建立新訂單（接收商品清單、收件資料、付款方式）
- GET /api/orders：取得會員自己的所有訂單（需登入）
- GET /api/orders/:id：取得單筆訂單詳細資料
- PUT /api/orders/:id/status：更新訂單狀態（需管理員權限）

說明訂單建立時需要驗證哪些資料，以及如何防止使用者竄改價格。
```

---

## 整合練習（綜合題）

### 綜合：從零建立完整電商網站
```
我要從零開始建立一個小型電商網站，技術選擇如下：
前端：React + CSS Modules
後端：Node.js + Express
資料儲存：JSON 檔案（不用資料庫）
部署：前端 Vercel、後端 Render

請幫我規劃整個專案的資料夾結構，包含：
- 前端 client/ 資料夾結構
- 後端 server/ 資料夾結構
- 需要哪些頁面元件
- 需要哪些後端 API 路由
- 資料格式（產品、訂單、會員各需要哪些欄位）

請用樹狀結構呈現，並說明每個資料夾和檔案的用途。
```
